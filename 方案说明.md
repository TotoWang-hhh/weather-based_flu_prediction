# 疾病预测方案说明

本文档基于当前项目中的代码实现和实际运行输出，对各个预测方案用自然语言进行说明，并概括其特点与当前表现。

## 研究目标

本项目的目标是利用天气数据和历史病例数据，预测每周新增病例数量。核心思路是把天气特征、历史周数特征和病例趋势一起纳入模型，比较不同建模方式在预测性能上的差异。

## 各方案说明

| 方案                     | 自然语言描述                                                                                  | 当前表现（以最近一次运行结果为准）                   |
| ---------------------- | --------------------------------------------------------------------------------------- | ----------------------------------- |
| 线性回归                   | 这是最基础的回归方法，把天气特征与病例数之间的关系看成一条整体直线。它计算简单、解释性强，但对复杂的季节波动和非线性变化往往拟合不足。                     | MAE 4033.69，RMSE 4779.09，R² -0.4049 |
| 随机森林                   | 这是一种基于多棵决策树的非线性模型。它可以自动学习不同特征之间的复杂组合关系，比线性回归更灵活，但对时间序列依赖的建模能力仍然有限。                      | MAE 3376.79，RMSE 3863.12，R² 0.0820  |
| 普通 LSTM                | 这是一个序列模型，能够把“过去一段时间的特征变化”看成一个时间序列来学习，从而捕捉病例随时间变化的趋势。                                    | MAE 2754.83，RMSE 3599.96，R² 0.2508  |
| History-weather LSTM   | 这种方案在 LSTM 的基础上，更强调“历史天气与病例信息一起输入”，也就是不仅看当前天气，还看前几周的天气和病例变化，从而更好地模拟传播过程的延迟效应。           | MAE 2861.64，RMSE 3314.88，R² 0.3241  |
| Notebook-inspired LSTM | 这是参考实验 notebook 思路实现的 LSTM 版本，结构更复杂，包含更深层的序列建模和更强的非线性表达能力。它的目标是让模型更像“专业实验中常见的深度序列模型”。   | MAE 3209.02，RMSE 3688.02，R² 0.1634  |
| Stacking meta-model    | 这是一个“二层融合”方案。先用多个基础模型分别给出预测，再由第二层模型学习如何把这些预测结果整合成最终输出。它的思想是“让不同模型各自负责不同方面，再由元模型决定最终取舍”。 | MAE 3010.50，RMSE 3550.01，R² 0.2715  |
| 加权集成模型                 | 这是一个更直接的融合方案。不同模型各自输出预测结果后，根据它们的表现给出不同权重，再做加权平均。它更像“多位专家投票”，通常比单模型更稳定。                  | MAE 3010.50，RMSE 3550.01，R² 0.2715  |

## 总体结论

1. 线性回归在这个任务上表现最差，说明病例变化并不是一个简单的线性关系。
2. LSTM 系列模型整体优于传统机器学习模型，说明时间依赖信息对疾病预测是有帮助的。
3. 融合模型在当前数据下比单一模型更有优势，尤其是加权集成模型表现较为稳健。
4. 从当前结果看，最有竞争力的方案是基于多个 LSTM 预测结果做融合的方案。

## 备注

- 代码入口在 main.py。
- 结果文件输出在 outputs 目录中，包括模型指标表、预测对比表和各类图表。
- 如果后续继续优化，可以考虑进一步调整特征工程、增加历史窗口长度，或尝试更强的第二层融合模型。
